node_modules/
dist/
src-tauri/target/
target/
.claude/
# codegraph 本地索引
.codegraph/
# 构建产物
release/
.DS_Store
*.log
*.tsbuildinfo

# 本地调试脚本（下划线前缀的临时 PowerShell 脚本，如查/杀僵尸进程锁 DLL，
# 仅本机排障用，不进仓库）
/_*.ps1

# Windows 保留设备名文件（git-bash 误产生：bash 不认 NUL/CON/PRN 是设备名，
# `>NUL` 重定向会创建物理文件而非丢弃输出；这些文件无法用常规方式删除，
# 且永远不该进仓库）
NUL
CON
PRN
AUX
COM1
COM2
COM3
COM4
LPT1
LPT2
LPT3
LPT4

# ZCode 本地会话状态
.zcode/

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
bower_components/
jspm_packages/
__pycache__/
site-packages/
venv/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.egg-info/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
